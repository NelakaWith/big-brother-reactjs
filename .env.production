# Production Environment Variables for Big Brother Dashboard
# Copy this file to your VPS and update the values

# JWT Secret Key (MUST be changed in production)
JWT_SECRET=your-super-secret-jwt-key-here

# Admin Authentication
AUTH_USERNAME=admin
AUTH_PASSWORD=your-secure-hashed-password-here

# Database Configuration
DB_PATH=./database/big-brother.db

# Application URLs
FRONTEND_URL=https://bigbro.nelakawithanage.com
BACKEND_URL=https://bigbro.nelakawithanage.com/api

# Application Ports (updated to avoid conflicts with other apps)
# Backend runs on 3001, Frontend runs on 3006 in production
PORT_BACKEND=3001
PORT_FRONTEND=3006

# Security
NODE_ENV=production

# Notes:
# - AUTH_PASSWORD should be bcrypt hashed
# - JWT_SECRET should be a long, random string
# - Update URLs to match your domain
# - These ports are configured to avoid conflicts with:
#   - faq-chatbot-backend (port 3000)
#   - gloire-road-map-backend (port 3005)